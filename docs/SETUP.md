# Setup

1. Install NeteaseMusic.app.
2. Install Node.js and Python 3.
3. Run `bash scripts/install.sh`.
4. Configure xiaomusic account/device using `config/xiaomusic-setting.example.json` as a guide.
5. Run `~/xiaomi-music/scripts/deploy.sh`.
6. Check `~/xiaomi-music/scripts/status.sh`.
